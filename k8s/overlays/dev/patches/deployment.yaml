apiVersion: apps/v1
kind: Deployment
metadata:
  name: portal
  namespace: lepa
spec:
  template:
    spec:
      containers:
        - name: lepa-portal
          image: nexlura/lepa-portal-dev:latest
          volumeMounts:
            - name: env-file
              mountPath: /.env
              subPath: .env
      volumes:
        - name: env-file
          configMap:
            name: nexlura-lepa-dev-env-file