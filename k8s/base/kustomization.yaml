apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lepa
resources:
  - service.yaml
  - deployment.yaml

labels:
  - pairs:
      app: lepa-portal

commonAnnotations:
  app: lepa-portal

# Removed nameSuffix for cleaner naming